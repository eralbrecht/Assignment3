using namespace std;
using "GenStack.h"
int main()
{
	countingStack = GenStack::GenStack();
	for (i = 0; i < analysisString.size(); i++)
	{
		currLett = analysisString[i];
		if (currLett == '[' || currLett == ']' || currLett == '{' || currLett == '}' || currLett == '(' || currLett == ')')
		{

		}
	}
}